pipeline {
    agent any

    environment {
        PATH = "/usr/bin:${env.PATH}" // Ajouter /usr/bin pour que mvn soit trouvé
    }

    stages {
        stage('Pull from Git') {
            steps {
                git branch: 'main', url: 'https://github.com/samderouich2000/Backend-CI-CD.git'
            }
        }

        stage('Clean Project') {
            steps {
                sh '/usr/bin/mvn clean'
            }
        }

        stage('Compile Project') {
            steps {
                sh '/usr/bin/mvn compile'
            }
        }

        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv('sq1') {
                    sh '/usr/bin/mvn sonar:sonar -Dsonar.projectKey=Backend-CI-CD -Dsonar.host.url=$SONAR_HOST_URL -Dsonar.login=$SONAR_AUTH_TOKEN -DskipTests'
                }
            }
        }

        stage('Package JAR') {
            steps {
                sh '/usr/bin/mvn package -DskipTests'
            }
        }
    }

    post {
        success {
            echo '✅ Build terminé avec succès !'
        }
        failure {
            echo '❌ Le build ou le scan SonarQube a échoué'
        }
    }
}
